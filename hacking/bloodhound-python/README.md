# BloodHound-Python Cheat Sheet

#### Obtener información del DC y guardar los archivos comprimidos
```
bloodhound-pyhton -u '<USER>' -p '<PASSWORD>' -d <DOMAIN> -ns <IP-DC> -c All --zip
```

#### Obtener información del DC
```
bloodhound-pyhton -u '<USER>' -p '<PASSWORD>' -d <DOMAIN> -ns <IP-DC> -c All
```

---

[:arrow_left: Regresar](https://github.com/m4lal0/cheatsheets)
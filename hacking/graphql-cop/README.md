# GraphQL Cop Cheat Sheet

#### Detectar vulnerabilidades en un endpoint de GraphQL
```
graphql-cop.py -t <URL> -o json
```

#### Detectar vulnerabilidades en un endpoint de GraphQL y guardar los resultado en formato JSON
```
graphql-cop.py -t <URL> -o json
```

---

[:arrow_left: Regresar](https://github.com/m4lal0/cheatsheets)